# Serve VialVault deep-link fallbacks instead of the SPA homepage.
# Targets live outside /v/ so they are not rematched by these rules
# and are not rewritten by Cloudflare Pages HTML Pretty URLs under /v/.
/v/v1/*  /vial-link/v1/  200
/v/*     /vial-link/unsupported/  200
